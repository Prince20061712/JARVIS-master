chromadb==0.4.22
sentence-transformers==2.2.2
# older sentence-transformers versions import `cached_download` from
# huggingface_hub, so we add a runtime shim in main.py.  No strict pin is
# required here; the broader <1.0 constraint is imposed by other packages.
#huggingface_hub<0.17
pypdf==3.17.4
langchain==0.1.0
faiss-cpu==1.7.4
tiktoken==0.5.1
unstructured==0.11.5
